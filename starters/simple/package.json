{
    "name": "monorepo",
    "scripts": {
        "build": "unbuild",
        "dev": "unbuild --stub",
        "release": "bumpp package.json --commit --push --tag"
    },
    "dependencies": {},
    "devDependencies": {
        "typescript:": "latest",
        "unbuild": "latest"
    }
}
